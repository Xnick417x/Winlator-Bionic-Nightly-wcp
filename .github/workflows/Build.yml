name: Build FEXCore Nightly (Arihany Exact)

on:
  workflow_dispatch:
  schedule:
    - cron: "0 17 * * *"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Install Dependencies
      # Sourced directly from: install-deps-ubuntu.sh
      run: |
        sudo apt-get update -y
        sudo apt-get install -y --no-install-recommends \
          ca-certificates curl xz-utils jq git glslang-tools \
          build-essential python3 python3-venv python3-pip \
          pkg-config cmake ninja-build zstd dos2unix perl tar unzip \
          llvm crossbuild-essential-arm64

    - name: Setup Toolchain
      # Sourced directly from: setup-llvm.sh
      run: |
        # Exact version Arihany uses
        LLVM_MINGW_TAG="20251104"
        TOOLCHAIN_DIR="/opt/llvm-mingw"
        
        # Download the specific Ubuntu 22.04 version (Crucial for correct binaries)
        wget -q "https://github.com/mstorsjo/llvm-mingw/releases/download/$LLVM_MINGW_TAG/llvm-mingw-$LLVM_MINGW_TAG-ucrt-ubuntu-22.04-x86_64.tar.xz" -O llvm.tar.xz
        
        sudo rm -rf "$TOOLCHAIN_DIR"
        sudo mkdir -p "$TOOLCHAIN_DIR"
        sudo tar -C "$TOOLCHAIN_DIR" --strip-components=1 -xJf llvm.tar.xz
        
        # Add to PATH (We trust this version has the correct binaries)
        echo "$TOOLCHAIN_DIR/bin" >> $GITHUB_PATH

    - name: Checkout FEX
      uses: actions/checkout@v4
      with:
        repository: FEX-Emu/FEX
        submodules: recursive
        fetch-depth: 0

    - name: Generate Version Name
      id: version
      run: |
        git config --global --add safe.directory '*'
        git fetch --tags --force
        LATEST_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "FEX-Unknown")
        COMMIT_HASH=$(git rev-parse --short HEAD)
        
        VERSION_NAME="${LATEST_TAG}-Nightly-${COMMIT_HASH}"
        
        echo "VERSION_NAME=${VERSION_NAME}" >> $GITHUB_ENV
        echo "COMMIT_HASH=${COMMIT_HASH}" >> $GITHUB_ENV

    - name: Build FEXCore
      # Logic sourced from x86-fexcore.yml
      run: |
        build_arch() {
          local triple="$1"
          local dest="$2"
          local build_dir="build-${triple}"
          
          echo "::group::Building $triple"
          
          # STRICT flags. No -static. No header patch.
          cmake -B "$build_dir" -G Ninja -Wno-dev \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_TOOLCHAIN_FILE=Data/CMake/toolchain_mingw.cmake \
            -DMINGW_TRIPLE="${triple}-w64-mingw32" \
            -DCMAKE_INSTALL_PREFIX=/usr \
            -DCMAKE_INSTALL_LIBDIR=/usr/lib/wine/aarch64-windows \
            -DENABLE_JEMALLOC_GLIBC_ALLOC=False \
            -DENABLE_LTO=False \
            -DENABLE_ASSERTIONS=False \
            -DBUILD_TESTING=False

          ninja -C "$build_dir"
          
          # Install to 'stage' folder (Matches Arihany's logic)
          DESTDIR="$(pwd)/$dest" ninja -C "$build_dir" install
          echo "::endgroup::"
        }

        # Build Pass 1: ARM64EC (Host)
        build_arch "arm64ec" "stage-ec"
        
        # Build Pass 2: AARCH64 (Bridge)
        build_arch "aarch64" "stage-wo"

    - name: Package
      run: |
        rm -rf final_package
        mkdir -p final_package/system32
        
        # FIX: Locate the DLLs in the STAGE folders where ninja installed them.
        echo "Searching for DLLs..."
        DLL_EC=$(find stage-ec -type f -name 'libFEXCore.dll' | head -n 1)
        DLL_WOW=$(find stage-wo -type f -name 'libFEXCore.dll' | head -n 1)
        
        if [ -f "$DLL_EC" ] && [ -f "$DLL_WOW" ]; then
            echo "Found EC: $DLL_EC"
            echo "Found WOW: $DLL_WOW"
            
            cp "$DLL_EC" final_package/system32/libarm64ecfex.dll
            cp "$DLL_WOW" final_package/system32/libwow64fex.dll
            
            # Using llvm-strip from the configured toolchain
            llvm-strip --strip-all final_package/system32/libarm64ecfex.dll
            llvm-strip --strip-all final_package/system32/libwow64fex.dll
        else
            echo "::error::Missing DLLs in stage folders!"
            echo "Contents of stage-ec:"
            ls -R stage-ec || true
            exit 1
        fi
        
        # Generate Profile JSON
        cat <<EOF > final_package/profile.json
        {
          "type": "FEXCore",
          "versionName": "${{ env.VERSION_NAME }}",
          "versionCode": ${{ github.run_number }},
          "description": "FEXCore Nightly\\nCommit: ${{ env.COMMIT_HASH }}\\nOptimized for Winlator/Ludashi",
          "author": "FEX-Emu / Gemini",
          "files": [
            {
              "source": "system32/libarm64ecfex.dll",
              "target": "\${system32}/libarm64ecfex.dll"
            },
            {
              "source": "system32/libwow64fex.dll",
              "target": "\${system32}/libwow64fex.dll"
            }
          ]
        }
        EOF

    - name: Create WCP
      run: |
        cd final_package
        tar -cJf ../${{ env.VERSION_NAME }}.wcp .

    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: fexcore-nightly
        path: '*.wcp'
